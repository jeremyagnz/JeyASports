[build]
  command = "npm run build"
  publish = "dist/jeyasports/browser"

[build.environment]
  NODE_VERSION = "22"

# Angular is a single page application: every deep link must fall back to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
